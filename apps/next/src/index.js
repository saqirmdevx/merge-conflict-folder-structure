const { printNode16PackageVersion } = require('node16Dep');
console.log('Version of frontend: ' + process.version);

printNode16PackageVersion();

// Calculate the sum of two numbers
const sum = (a, b) => a + b;
console.log(sum(5, 10));
